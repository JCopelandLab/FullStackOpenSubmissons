import ReactDOM from "react-dom/client";
import App from "./App.jsx";

// these imports act as the

ReactDOM.createRoot(document.getElementById("root")).render(<App />);
//this line states: ReactDom, create a root connection to the HTML element with id: "root" and render the App component within it
